//
//  ContentView.swift
//  LOTRConverter
//
//  Created by SEMEQ  on 30/01/24.
//

import SwiftUI

struct ContentView: View {
    var body: some View {
        ZStack {
            // bg image
            VStack {
                // pony image
                // currency exchange
                // conversion
                HStack {
                    // left
                    VStack {
                        // currency
                        HStack {
                            // currency img
                            // currency text
                        }
                        // text
                    }
                    // equal
                    // riqht
                    VStack {
                        // currency
                        HStack {
                            // currency text
                            // currency img
                        }
                        // text
                    }
                }
                // info btn
            }
        }
    }
}

#Preview {
    ContentView()
}

